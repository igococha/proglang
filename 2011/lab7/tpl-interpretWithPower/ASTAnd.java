/* Generated By:JJTree: Do not edit this line. ASTAnd.java */

public class ASTAnd extends SimpleNode {
  public ASTAnd(int id) {
    super(id);
  }

  public ASTAnd(TPLParser p, int id) {
    super(p, id);
  }

  /* Added by DB to perform identification and type checking. */

  public void identification () {
     jjtGetChild(0).identification();
     jjtGetChild(1).identification();
  }

  public void typecheck () {
    jjtGetChild(0).typecheck();
    jjtGetChild(1).typecheck();
    if (!(jjtGetChild(0).GetNodeType() == TPLTypes.boolType &&
          jjtGetChild(1).GetNodeType() == TPLTypes.boolType))
       System.out.println("TPL Typechecker: and of non-bools.");
    NodeType = TPLTypes.boolType;
  }

  /* Added by DB to perform interpreting. */

  public void interpret()
  {
     boolean left, right;
     jjtGetChild(0).interpret();

     left = ((Boolean)stack.pop()).booleanValue();
     if (!left)
     {
        stack.push(new Boolean(false));
        return;
     }

     jjtGetChild(1).interpret();
     right = ((Boolean)stack.pop()).booleanValue();
     stack.push(new Boolean(right));
  }

}

